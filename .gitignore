# Rust build artifacts
/target

# Backtrace / debug files
*.pdb

# IDE / editor
/.idea
/.vscode
*.swp

# OS cruft
.DS_Store

# Persisted onion identity keys (never commit a secret key)
*.key

# Persisted address book (your social graph — keep it private)
contacts.json
